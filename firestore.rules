rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // доступ только владельцу документа (UID == Telegram ID)
      allow read, write: if request.auth.uid == userId;
    }
  }
}